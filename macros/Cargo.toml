[package]
name = "macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = { version = "1.0.103", features = ["proc-macro"] }
syn = { version = "2.0.111", features = ["full"] }
